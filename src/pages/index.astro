---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
---

<Layout title="Aaren Arasaratnam">
	<main>
		<div class="welcome-text">
			<h3>Hello,</h3>
			<h1>My name's<span class="name"> Aaren.</span></h1>
			<h2>How's it going?</h2>
		</div>
		<img src="me.jpg" />
		<p>This is a portfolio site, if you haven't guessed already.</p>
		<p>
			Not quite sure what I should put here, to be frank, but maybe, just maybe,
			I'll figure it all out along the way.
		</p>
		<p>Or I don't, that's okay too.</p>
		<p>Let this dancing thing immerse you in the meantime:</p>
		<!-- <Card type="ace-of-spades" /> -->
	</main>
</Layout>

<style lang="scss">
	@import '../global.scss';

	main {
		.welcome-text {
			text-align: left;
			line-height: 0.9;
			color: $secondary;
			z-index: 5;
			position: absolute;
			inset: 6rem auto auto 8rem;

			h1 {
				font-size: 6rem;
				font-weight: 700;
				line-height: 1;

				.name {
					color: rgba($accent-1, 0.4);
				}
			}

			h2 {
				font-size: 3rem;
				font-weight: 400;
				margin-top: 0.5em;
			}

			h3 {
				font-size: 2rem;
				font-weight: 600;
				margin-bottom: 0.5em;
			}
		}
		img {
			width: 50%;
			filter: contrast(0%) opacity(35%) blur(4px)
				drop-shadow(30em 1px 5px $accent-3);
			z-index: -50;
			position: absolute;
			top: 0;
		}
		p {
			position: relative;
			inset: 30rem auto auto 8rem;
			width: 50%;
			font-size: 1.5rem;
			line-height: 1.5;
			color: $primary;
		}
	}
</style>
